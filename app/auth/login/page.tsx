import { LoginForm } from "@/components/login/loginForm";
export default function LoginPage() {
  return (
    <div style={{ paddingTop: "8rem" }} className="flex justify-center">
      <LoginForm />
    </div>
  );
}